services:
  frontend-tests:
    build:
      context: ./frontend
    command: npm run test

  pytest:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "pip install -r requirements-dev.txt && pytest"
